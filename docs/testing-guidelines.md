# Testing Guidelines

- Prefer widget tests for UI behavior in reusable components
- Add service-level tests around parsing and transformation logic
- Keep tests deterministic and independent from network side effects
- Run `flutter test` before opening a PR
- Keep test names behavior-focused and readable

This guide introduces no code-path changes.
