# Firebase Configuration Notes

- Firebase project settings are defined in `firebase.json` and security rules files under `flut/`
- Platform bootstrap config exists in `flut/lib/firebase_options.dart`
- Android and iOS Google service files must match the active Firebase project
- Keep Firestore and Storage rules reviewed before deployment

No production logic is modified by this document.
