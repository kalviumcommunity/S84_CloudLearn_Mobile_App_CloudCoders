# Troubleshooting Guide

Common issues:
- `flutter pub get` fails: verify SDK path and internet connectivity
- Firebase auth errors: confirm platform config files and package IDs
- Build failures: clean with `flutter clean` then retry
- Analyzer warnings: run `flutter analyze` and address reported items

This guide is documentation-only and safe for runtime behavior.
